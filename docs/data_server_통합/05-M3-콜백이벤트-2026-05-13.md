# M3 (= D) — 콜백 이벤트 (추천 품질 환류)

- **일자**: 2026-05-13
- **상위 컨텍스트**: A(카드 강화)·B(트렌드 페이지) 직후. 사용자가 추천을 어떻게 사용했는지 pet-data-api 로 회신해서, `InteractionHistorySignal` 신호가 0→실값으로 살아나도록 한다. 이게 없으면 가이드 §FAQ 그대로 "history 신호가 영원히 0" — 시간 누적 학습 효과 무력화.
- **가이드**: [`../PETORY-INTEGRATION.md`](../PETORY-INTEGRATION.md) §M3, §6

## 1. 왜 (배경)

가이드 §6 표:

| 사용자 행동         | 이벤트  | 권장도                         |
| ------------------- | ------- | ------------------------------ |
| 추천 카드 첫 노출   | `view`  | 선택 (보내면 노출률 계산 가능) |
| 카드/시설 클릭      | `click` | **권장**                       |
| 예약/전화 같은 전환 | `book`  | 권장 (Petory 미구현)           |

추천 카드는 A 작업으로 풍부해졌고, B 작업으로 트렌드 탐색 경로가 열렸다. 이제 사용자가 어떤 시설을 실제로 클릭하는지 데이터로 흐르게 해서, 추천 랭커가 `recommendation_log` ↔ `facility_interactions` JOIN 으로 "많이 보는 곳" 신호를 만들도록 한다.

## 2. 무엇을 (변경 범위)

### 2.1 백엔드 — 신규 1, 수정 4

| 파일                                    | 변경                                                                                                                                                                               |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `dto/RecommendEventRequest.java` (신규) | `{request_id, events:[{facility_id?, source_id?, event, occurred_at, user_ref?}]}`                                                                                                 |
| `dto/RecommendResponse.java`            | `FacilityItem` 에 `id`, `source_id` 추가. pet-data-api 응답이 줄 때만 채워짐(없어도 null 안전)                                                                                     |
| `client/PetDataApiClient.java`          | `sendEvents(RecommendEventRequest)` 추가. `toBodilessEntity()` 로 202 응답 본문 무시. **예외를 다시 던지지 않음** — fire-and-forget. 본 추천과 같은 3s 타임아웃 클라이언트 재사용. |
| `service/RecommendService.java`         | `recordEvents(userId, body)` 추가. **`user_ref` 는 백엔드가 자동 채움** — Petory `userId` 의 SHA-256 해시 12자(`petory-xxxxxxxxxxxx`). 원본 ID 노출 없음.                          |
| `controller/RecommendController.java`   | `POST /api/recommend/events`. 응답은 항상 `202 Accepted` (가이드 §6 그대로). 본 추천과 같은 인증 정책.                                                                             |

### 2.2 프론트엔드 — 수정 2

| 파일                                         | 변경                                                                                                                                                                                                                                                     |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `api/recommendApi.js`                        | `sendEvents({requestId, events})` 추가. fire-and-forget 패턴 (`.catch(() => {})`)                                                                                                                                                                        |
| `components/Recommendation/RecommendCard.js` | (1) `useEffect([data?.request_id])` 로 **새 추천 도착 시 view 배치 1콜**. (2) `handleFacilityClick(f)` 로 **시설 항목 클릭 시 즉시 click 이벤트 전송**. (3) `id` 또는 `source_id` 가 있을 때만 cursor:pointer + hover 효과 + onClick — 시각·기능 일관성. |

> **개인정보 보호**: 프론트는 `user_ref` 를 보내지 않는다. 백엔드가 한 군데서 일관되게 해시값으로 채운다. 클라이언트 코드 어디에도 사용자 식별 정보가 노출되지 않는다.

## 3. 어떻게 (핵심 코드)

### 3.1 백엔드 — 해시 user_ref

```67:78:backend/main/java/com/linkup/Petory/domain/recommendation/service/RecommendService.java
    private static String hashUserId(String userId) {
        if (userId == null) {
            return null;
        }
        try {
            MessageDigest md = MessageDigest.getInstance("SHA-256");
            byte[] hash = md.digest(userId.getBytes(StandardCharsets.UTF_8));
            return "petory-" + HexFormat.of().formatHex(hash).substring(0, 12);
        } catch (NoSuchAlgorithmException e) {
            // SHA-256 은 JRE 표준. 실제 발생 가능성 없음 — 폴백은 원본 ID 비노출 위해 그냥 null.
            return null;
        }
    }
```

### 3.2 백엔드 — fire-and-forget 클라이언트

```83:103:backend/main/java/com/linkup/Petory/domain/recommendation/client/PetDataApiClient.java
    public void sendEvents(RecommendEventRequest request) {
        // 가이드 §6: 응답은 항상 202 Accepted. fire-and-forget — 실패해도 사용자 액션을 막지 않는다.
        String requestId = newRequestId();
        try {
            String requestJson = objectMapper.writeValueAsString(request);
            log.info("[PetDataApiClient/events] 요청 전송 reqId={} → {}", requestId, requestJson);

            recommendClient.post()
                    .uri("/events/recommendation")
                    .contentType(MediaType.APPLICATION_JSON)
                    .header("X-Request-Id", requestId)
                    .body(request)
                    .retrieve()
                    .toBodilessEntity();

            log.info("[PetDataApiClient/events] 응답 수신 reqId={}", requestId);
        } catch (Exception e) {
            log.warn("[PetDataApiClient/events] 실패(무시) reqId={} {} — {}",
                    requestId, e.getClass().getSimpleName(), e.getMessage());
        }
    }
```

### 3.3 프론트 — view 배치 1콜 + click 즉시 전송

```168:186:frontend/src/components/Recommendation/RecommendCard.js
  // 추천 카드가 마운트되고 데이터가 도착했을 때 view 이벤트 배치 1콜 전송.
  // request_id 가 바뀔 때만 (= 새 추천 결과가 도착했을 때만) 한 번 발송.
  useEffect(() => {
    const requestId = data?.request_id;
    const facilities = data?.facilities ?? [];
    if (!requestId || facilities.length === 0) return;

    const occurredAt = new Date().toISOString();
    const events = facilities.map((f) => ({
      facility_id: f.id ?? null,
      source_id: f.source_id ?? null,
      event: 'view',
      occurred_at: occurredAt,
    }));
    sendRecommendEvents(requestId, events);
  }, [data?.request_id]); // eslint-disable-line react-hooks/exhaustive-deps

  const handleFacilityClick = (f) => {
    const requestId = data?.request_id;
    sendRecommendEvents(requestId, [{
      facility_id: f.id ?? null,
      source_id: f.source_id ?? null,
      event: 'click',
      occurred_at: new Date().toISOString(),
    }]);
  };
```

```157:166:frontend/src/components/Recommendation/RecommendCard.js
// 콜백 이벤트 전송 헬퍼. fire-and-forget — 실패해도 무시.
function sendRecommendEvents(requestId, eventList) {
  if (!requestId || !eventList || eventList.length === 0) return;
  // facility_id 또는 source_id 가 있는 이벤트만 보냄 (가이드 §6)
  const usable = eventList.filter(
    (e) => e.facility_id != null || e.source_id != null
  );
  if (usable.length === 0) return;
  recommendApi.sendEvents({ requestId, events: usable }).catch(() => {});
}
```

## 4. 검증

| 항목                                             | 결과                                                                   |
| ------------------------------------------------ | ---------------------------------------------------------------------- |
| `./gradlew compileJava`                          | ✅ exit 0                                                              |
| 백엔드 린터 오류                                 | ✅ 없음                                                                |
| 프론트 ESLint                                    | ✅ 없음                                                                |
| `id`/`source_id` 가 없는 응답(M0~M2 호환) 안전성 | ✅ `clickable=false` 분기로 onClick 미부착, view 배치도 filter 로 제외 |
| pet-data-api 다운 시 사용자 영향                 | ✅ `.catch(() => {})` + 백엔드 `try/catch` 두 단계 흡수. 카드 UI 정상  |
| 개인정보 노출                                    | ✅ 프론트는 `user_ref` 무전송. 백엔드 해시로만 가공                    |

### 런타임 확인 시나리오

1. `npm start` → 추천 카드 진입 → 네트워크 탭에서 `POST /api/recommend/events` 가 `event:"view"` × N 으로 1회 발송되는지
2. 시설 항목에 hover → 배경색 변화 + cursor:pointer
3. 시설 항목 클릭 → `POST /api/recommend/events` 가 `event:"click"` × 1 로 발송
4. pet-data-api 가 다운된 상태에서도 카드 UI 정상, 콘솔에 `.catch` 무시 로그만
5. 백엔드 로그에 `user_ref="petory-..."` 형태로 해시값이 찍히고, 원본 userId 가 페이로드에 없음

## 5. A/B 작업의 영향 반영

| A/B 결정                                              | M3 반영                                                                                                                                      |
| ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| A: 시설 항목이 추천도·뱃지·칩으로 인터랙티브하게 보임 | onClick 부착이 자연스러움. cursor:pointer + hover 효과로 클릭 가능을 시각적으로도 알림                                                       |
| A: primary 색 칩으로 시설↔트렌드 연결 표현            | click 이벤트가 누적되면 다음 추천에서 `history` 신호가 살아 reasons 에 `history` 라벨이 등장 → A 의 "많이 보는 곳" 칩이 실제로 나타나기 시작 |
| B: 트렌드 카드 → 별도 진입 동선                       | 트렌드 카드 자체에는 콜백 미부착(현재). 사용자가 키워드를 클릭하는 행동은 시설 클릭과 다른 의미라 별도 설계 필요. M3 범위 밖.                |

## 6. 의도적으로 안 한 것

| 항목                                          | 이유                                                                                                                               |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 디바운스/배치 큐                              | view 는 카드 mount 시 1콜로 이미 배치. click 은 사용자가 1초에 여러 번 클릭할 시나리오 거의 없음. 큐 인프라 추가는 외과적 범위 밖. |
| `book` 이벤트                                 | Petory 에 시설 예약/전화 연결 기능 없음. 도입 시 별도 PR.                                                                          |
| 트렌드 카드 클릭 트래킹                       | 이벤트 의미(시설 ↔ 키워드)가 달라 별도 설계 필요. M3 콜백 스펙은 시설 단위.                                                        |
| 클릭 후 페이지 이동/모달                      | 콜백은 학습용 데이터. 사용자 경험상 클릭 후 어떤 액션을 줄지는 별도 UX 결정. 지금은 데이터만 흐르게.                               |
| `accepted/skipped` 카운트를 클라이언트에 노출 | 가이드: "4xx/5xx 가 아니면 신경 안 써도 됨". 단순화.                                                                               |

## 7. 다음 단계 — 보류된 M2 (`/facilities/search`) 재판단

A·B·D 가 모두 끝났으니 가이드의 마지막 미완료 항목인 `/facilities/search` 도입 여부를 사용자와 최종 확정한다.

### 현재 권장 (재확인)

> **도입하지 않거나, 도입해도 부가 기능에만** — 일반 "주변 시설 찾기" 페이지는 Petory 자체 `locationServiceApi` 유지.

| 이유                                                                                                                   |
| ---------------------------------------------------------------------------------------------------------------------- |
| ① A 작업으로 추천 카드 자체가 시설 표시·이유·점수까지 풍부해졌음. 사용자가 "주변 시설" 을 찾을 1차 진입점은 추천 카드. |
| ② B 작업으로 비-시설 카테고리는 트렌드 페이지로 분리. `/facilities/search` 가 채울 빈자리가 줄어듦.                    |
| ③ Petory 자체 시설 DB 는 사용자 리뷰·평점·즐겨찾기와 직접 조인 가능. 마이그레이션 시 이 조인이 깨짐.                   |
| ④ D 콜백이 들어가면서 추천 품질이 시간이 갈수록 좋아질 것 — 일반 검색 마이그레이션의 우선순위가 더 떨어짐.             |

### 옵션 (사용자 결정)

- **(가)** 보류 유지 (권장)
- **(나)** 관리자 대시보드 같은 부가 기능에만 `/facilities/search` 도입
- **(다)** 일반 사용자 페이지까지 마이그레이션 (가이드 그대로) — 비용·리스크 큼

> 사용자가 결정해주면 ADR(Architecture Decision Record) 로 정식 문서화.
